#!/bin/bash
while getopts "s:" arg; do
  if [ $arg == "s" ] && [ $OPTARG == "steamos-updatelevel" ]; then
    echo "Version: $(lsb_release -r -s)"
  elif [ -e /usr/bin/dpkg-query ]; then
    /usr/bin/dpkg-query $@
  else
    echo "dpkg-query doesn't seem to be installed"
    exit 1
  fi
done
